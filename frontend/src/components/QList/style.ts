import styled from 'styled-components';

export const RemoveIcon = styled.button`
  position: absolute;
  right: 10px;
  top: 15px;
  background: none;
  border: none;

  &:hover {
    color: #23527c;
  }
`;
